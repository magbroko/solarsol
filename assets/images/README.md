# Images

Place local image assets here (logos, icons, photos).

The site currently uses Unsplash CDN URLs for hero and project images. To use local images instead, update the `src` attributes in `index.html` to point to files in this folder, e.g.:

```
assets/images/hero-bg.jpg
assets/images/logo.svg
```
